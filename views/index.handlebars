<img src="/public/assets/img/burger.png" atl="image-burger" height="200" width="200">
<h1>Eat Da Burger</h1>


<h2>Your List of Burgers</h2>
{{!-- Unless the burgers have not been devoured --}}
<ul>
    {{#each burger}}
        {{#unless devoured}}
            {{> burger/burgers-block devoured=true}}
        {{/unless}}
    {{/each}}
</ul>

<h2>Was the Burger Eaten??</h2>
<ul>
    {{#each burger}}
        {{#if devoured}}
            {{> burger/burgers-block devoured=false}}
        {{/if}}
    {{/each}}
</ul>

<h2>Add Your Favorite Burger Here</h2>

<form class="create-form">

    <div class="form-group">
        <label for="exampleFormControlTextarea1"></label>
        <textarea class="form-control" id="exampleFormControlTextarea1" rows="3"></textarea>
    </div>

    <button type="submit">Add Da Burger</button>

    </div>
</form>